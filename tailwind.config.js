/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        'pool-blue': '#0077b6', // Deep blue
        'pool-ice': '#f0f9ff',  // Ice white (sky-50 equivalent)
        'pool-dark': '#1e293b', // Anthracite (slate-800 equivalent)
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
      animation: {
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      }
    },
  },
  plugins: [],
}
